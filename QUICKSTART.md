# ⚡ Быстрый старт

Минимальная инструкция для запуска бота за 5 минут.

## 1️⃣ Установка

```bash
# Клонировать репозиторий (или распаковать архив)
cd content-aggregator-bot

# Установить зависимости
uv sync
```

## 2️⃣ Настройка Telegram

### Создать бота

1. Найти [@BotFather](https://t.me/BotFather) в Telegram
2. Отправить `/newbot`
3. Скопировать токен → `TELEGRAM_BOT_TOKEN`

### Создать группу

1. Создать группу в Telegram
2. Добавить бота в группу **как администратора**
3. Отправить сообщение в группу
4. Открыть: `https://api.telegram.org/bot<TOKEN>/getUpdates`
5. Найти `"chat":{"id": -100...}` → `TELEGRAM_TARGET_CHAT_ID`

### Получить API credentials

1. Открыть <https://my.telegram.org>
2. Войти → API development tools
3. Создать приложение
4. Скопировать `api_id` → `TELEGRAM_API_ID`
5. Скопировать `api_hash` → `TELEGRAM_API_HASH`

## 3️⃣ Настройка VK

### Получить токен

1. Открыть <https://vkhost.github.io/>
2. Выбрать права: `wall`, `groups`, `photos`, `video`, `offline`
3. Нажать "Get Token"
4. Разрешить доступ
5. Скопировать токен → `VK_ACCESS_TOKEN`

## 4️⃣ Настройка .env

```bash
# Скопировать пример
cp .env.example .env

# Отредактировать .env
nano .env  # или используйте любой редактор
```

Заполнить:

```env
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_TARGET_CHAT_ID=-100ваш_id
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
VK_ACCESS_TOKEN=ваш_vk_токен
```

## 5️⃣ Настройка источников

Отредактировать `config/sources.yaml`:

```yaml
sources:
  - type: telegram
    url: https://t.me/your_channel
    enabled: true

  - type: vk
    url: https://vk.com/your_group
    enabled: true
```

## 6️⃣ Первый запуск

```bash
# Активировать окружение
source .venv/bin/activate  # Linux/macOS
# или
.venv\Scripts\activate  # Windows

# Запустить
python -m app
```

При первом запуске:

1. Ввести номер телефона: `+79123456789`
2. Ввести код из Telegram
3. (Если есть) Ввести пароль 2FA

## 7️⃣ Проверка

Бот должен:

- ✅ Успешно подключиться к Telegram
- ✅ Успешно подключиться к VK
- ✅ Начать мониторинг источников
- ✅ Публиковать новые посты в группу

## 🆘 Проблемы?

- **"Unauthorized"** → Проверьте `TELEGRAM_BOT_TOKEN`
- **"Chat not found"** → Проверьте `TELEGRAM_TARGET_CHAT_ID` (должен начинаться с `-100`)
- **"Bot is not admin"** → Сделайте бота администратором группы
- **"Invalid access_token"** → Получите новый VK токен
- **Бот просит код** → Введите номер телефона в международном формате

## 📖 Подробная документация

- **[README.md](README.md)** - Полная документация
- **[SETUP_TELEGRAM.md](SETUP_TELEGRAM.md)** - Подробная настройка Telegram
- **[SETUP_VK.md](SETUP_VK.md)** - Подробная настройка VK

# 🤖 Content Aggregator Bot

Автоматический агрегатор контента из социальных сетей (Telegram, VK) с репостом в единый Telegram канал или группу.

## 📋 Возможности

- ✅ **Мультиплатформенность:** Мониторинг Telegram каналов и VK сообществ.
- ✅ **Умная дедупликация:** Проверка контента на дубликаты (хэширование текста и медиа) для исключения повторов.
- ✅ **Rich Media:** Поддержка фото, видео, альбомов и документов.
- ✅ **Безопасность:** Встроенный Rate Limiter и ротация User-Agent для защиты от блокировок.
- ✅ **Наблюдаемость:** Подробное логирование в консоль и файл с автоматической ротацией.
- ✅ **Расширяемость:** Модульная архитектура, позволяющая легко добавлять новые источники (YouTube, Instagram и др.).

---

## 🚀 Быстрый старт

### 1. Установка

Проект использует [uv](https://github.com/astral-sh/uv) для управления зависимостями.

```bash
# Клонируйте репозиторий
git clone https://github.com/yourdisenchantment/content-aggregator-bot
cd content-aggregator-bot

# Установите зависимости и виртуальное окружение
uv sync
```

### 2. Конфигурация окружения

Создайте файл настроек из примера:

```bash
cp .env.example .env
```

Заполните `.env` вашими данными (токены и ключи). Подробная инструкция по их получению находится ниже в разделе **"⚙️ Подробная настройка"**.

### 3. Настройка источников

Отредактируйте файл `config/sources.yaml`, добавив интересные вам каналы:

```yaml
sources:
  - type: telegram
    url: https://t.me/example_channel
    enabled: true

  - type: vk
    url: https://vk.com/example_public
    enabled: true
```

### 4. Запуск

Используйте `uv` для запуска бота:

```bash
# Обычный запуск (использует скрипт из pyproject.toml)
uv run bot

# Или напрямую через модуль
uv run python -m app
```

> **Важно:** При первом запуске бот запросит номер телефона и код подтверждения (для библиотеки Telethon/Telegram Client). Сессия сохранится локально в файл `aggregator_session.session`.

---

## 🛠 Аргументы командной строки

Бот поддерживает гибкую настройку запуска через CLI без необходимости менять код или `.env`.

### Управление логированием

| Флаг              | Описание                                                     | Пример                       |
| ----------------- | ------------------------------------------------------------ | ---------------------------- |
| `-v`, `--verbose` | **Debug режим.** Вывод всей отладочной информации в консоль. | `uv run bot -v`              |
| `-q`, `--quiet`   | **Тихий режим.** В консоль выводятся только ошибки.          | `uv run bot -q`              |
| `--level`         | Установка конкретного уровня (INFO, WARNING, ERROR).         | `uv run bot --level WARNING` |
| `--no-file`       | **Отключить запись в файл.** Логи будут только в консоли.    | `uv run bot --no-file`       |

**Примеры комбинаций:**

```bash
# Максимальная детализация в консоли, но без сохранения логов на диск
uv run bot -v --no-file

# Только критические ошибки
uv run bot --level ERROR
```

---

## ⚙️ Подробная настройка

### 📱 Настройка Telegram

#### Шаг 1: Создание бота (Публикатор)

1. Напишите [@BotFather](https://t.me/BotFather) команду `/newbot`.
2. Введите имя и username бота.
3. Скопируйте полученный токен в `.env` -> `TELEGRAM_BOT_TOKEN`.

#### Шаг 2: Настройка целевой группы

1. Создайте канал или группу в Telegram.
2. Добавьте туда созданного бота **как администратора** (права на публикацию сообщений).
3. Узнайте ID группы. Самый простой способ:
   - Добавьте в группу бота [@userinfobot](https://t.me/userinfobot).
   - Он пришлет ID (для супергрупп начинается с `-100`).
   - Удалите @userinfobot.
4. Запишите ID в `.env` -> `TELEGRAM_TARGET_CHAT_ID`.

#### Шаг 3: API Credentials (Скрапер)

Это необходимо для работы клиента (Telethon), который читает посты из чужих каналов как обычный пользователь.

1. Зайдите на [my.telegram.org](https://my.telegram.org).
2. Авторизуйтесь по номеру телефона.
3. Перейдите в **API development tools**.
4. Создайте приложение (App title и Short name могут быть любыми).
5. Скопируйте `api_id` и `api_hash` в `.env`.

---

### 🌐 Настройка VK

#### Получение токена доступа

Для чтения публичных групп достаточно User Token.

**Способ 1 (Быстрый):**

1. Откройте [VKHost.github.io](https://vkhost.github.io/).
2. Выберите настройки: `wall`, `groups`, `photos`, `video`, `offline`.
3. Нажмите "Get Token".
4. Скопируйте часть URL из адресной строки (между `access_token=` и `&expires_in`).
5. Вставьте токен в `.env` -> `VK_ACCESS_TOKEN`.

**Способ 2 (Официальный):**
Создайте Standalone-приложение в [VK Apps](https://vk.com/apps?act=manage) и получите токен через OAuth с правами `wall,offline`.

---

## 🔧 Конфигурация (.env)

Полный список переменных окружения:

| Переменная                       | Описание                       | Значение по умолчанию                      |
| -------------------------------- | ------------------------------ | ------------------------------------------ |
| `TELEGRAM_BOT_TOKEN`             | Токен бота от BotFather        | -                                          |
| `TELEGRAM_TARGET_CHAT_ID`        | ID чата куда постить (-100...) | -                                          |
| `TELEGRAM_API_ID`                | Telegram API ID                | -                                          |
| `TELEGRAM_API_HASH`              | Telegram API Hash              | -                                          |
| `TELEGRAM_SESSION_NAME`          | Имя файла сессии               | `aggregator_session`                       |
| `VK_ACCESS_TOKEN`                | Токен VK                       | -                                          |
| `VK_API_VERSION`                 | Версия API VK                  | `5.131`                                    |
| `DATABASE_URL`                   | Путь к БД (SQLite)             | `sqlite+aiosqlite:///./data/aggregator.db` |
| `LOG_LEVEL`                      | Уровень логов консоли          | `INFO`                                     |
| `LOG_FILE`                       | Путь к файлу логов             | `logs/bot.log`                             |
| `SCRAPE_INTERVAL_SECONDS`        | Пауза между проверками         | `60`                                       |
| `RATE_LIMIT_REQUESTS_PER_MINUTE` | Лимит запросов к API           | `30`                                       |
| `POST_CHECK_DELAY_SECONDS`       | Задержка перед репостом        | `600` (10 мин)                             |

---

## 📁 Структура проекта

```text
content-aggregator-bot/
├── app/
│   ├── database/       # Слой работы с БД (SQLite + SQLAlchemy)
│   ├── models/         # Pydantic модели данных
│   ├── publishers/     # Логика отправки сообщений (Telegram Bot API)
│   ├── scrapers/       # Логика сбора данных (Telegram Client, VK API)
│   └── utils/          # Логгер, валидаторы, rate limiter
├── config/
│   └── sources.yaml    # Список источников
├── logs/               # Файлы логов (создаются автоматически)
├── data/               # БД и сессии (создаются автоматически)
└── pyproject.toml      # Зависимости и скрипты
```

## 🐛 Решение проблем

### Бот не запускается

Убедитесь, что зависимости синхронизированы:

```bash
uv sync
```

### Ошибка авторизации Telegram (AuthKeyError)

Если возникает ошибка сессии или вы поменяли API ID:

1. Удалите файл `aggregator_session.session`.
2. Запустите бота заново и пройдите авторизацию по номеру телефона.

### Rate Limit Exceeded

Если Telegram или VK временно блокируют запросы ("Too many requests"):

1. Увеличьте `SCRAPE_INTERVAL_SECONDS` в `.env`.
2. Уменьшите количество источников в `sources.yaml`.

### Посты не публикуются

1. Проверьте логи: `uv run bot -v`.
2. Убедитесь, что бот является администратором в целевом канале.
3. Проверьте правильность `TELEGRAM_TARGET_CHAT_ID` (должен начинаться с `-100`).

## 📝 Добавление новых платформ

1. Создайте новый скрапер в `app/scrapers/new_platform.py`, унаследовав класс `BaseScraper`.
2. Добавьте новый тип платформы в `app/models/content.py` -> `PlatformType`.
3. Зарегистрируйте скрапер в фабрике `app/scrapers/factory.py`.
4. Добавьте валидацию URL в `app/utils/validators.py`.

## 🤝 Поддержка

При возникновении ошибок:

1. Запустите бота с флагом отладки: `uv run bot -v`
2. Изучите вывод в консоли и файл `logs/bot.log`.
3. Создайте Issue в репозитории с описанием ошибки и логами.

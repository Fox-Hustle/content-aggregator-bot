# 🤖 Content Aggregator Bot

Автоматический агрегатор контента из социальных сетей (Telegram, VK) с репостом в Telegram группу.

## 📋 Возможности

- ✅ Мониторинг Telegram каналов
- ✅ Мониторинг VK сообществ
- ✅ Автоматическая дедупликация постов
- ✅ Публикация с медиафайлами (фото, видео, документы)
- ✅ Rate limiting для защиты от блокировок
- ✅ Подробное логирование
- ✅ Модульная архитектура для добавления новых платформ

## 🚀 Быстрый старт

### 1. Установка зависимостей

Убедитесь, что у вас установлен Python 3.14+ и uv:

```bash
# Клонируйте репозиторий
git clone https://github.com/yourdisenchantment/content-aggregator-bot
cd content-aggregator-bot

# Установите зависимости с помощью uv
uv sync
```

### 2. Настройка переменных окружения

Скопируйте пример и заполните своими данными:

```bash
cp .env.example .env
```

Откройте `.env` и заполните все необходимые параметры (см. раздел "Настройка" ниже).

### 3. Настройка источников

Отредактируйте `config/sources.yaml` и добавьте источники для мониторинга:

```yaml
sources:
  - type: telegram
    url: https://t.me/your_channel
    enabled: true

  - type: vk
    url: https://vk.com/your_public
    enabled: true
```

### 4. Запуск

```bash
# Активируйте виртуальное окружение
source .venv/bin/activate  # Linux/macOS
# или
.venv\Scripts\activate  # Windows

# Запустите бота
python -m app
```

## ⚙️ Подробная настройка

### Настройка Telegram Bot

#### Шаг 1: Создание бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Придумайте имя и username для бота
4. Скопируйте полученный токен → это ваш `TELEGRAM_BOT_TOKEN`

#### Шаг 2: Добавление бота в группу

1. Создайте группу в Telegram (или используйте существующую)
2. Добавьте вашего бота в группу как администратора
3. Дайте боту права:
   - ✅ Отправка сообщений
   - ✅ Отправка медиа
   - ✅ Отправка других сообщений

#### Шаг 3: Получение ID группы

Есть несколько способов:

**Способ 1: Через API**

```bash
# Отправьте любое сообщение в группу, затем выполните:
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates

# Найдите в ответе поле "chat":{"id": -100xxxxxxxxx}
# Это и есть ваш TELEGRAM_TARGET_CHAT_ID
```

**Способ 2: Через @userinfobot**

1. Добавьте [@userinfobot](https://t.me/userinfobot) в группу
2. Бот отправит ID группы
3. Удалите бота из группы

#### Шаг 4: Получение API credentials для чтения каналов

1. Откройте <https://my.telegram.org>
2. Войдите с номером телефона
3. Перейдите в "API development tools"
4. Создайте приложение (если еще не создано)
5. Скопируйте:
   - `api_id` → `TELEGRAM_API_ID`
   - `api_hash` → `TELEGRAM_API_HASH`

#### Шаг 5: Первая авторизация клиента

При первом запуске бот попросит ввести код авторизации:

```bash
python -m app

# Вы увидите:
# Please enter your phone (or bot token): +79XXXXXXXXX
# Введите ваш номер телефона

# Please enter the code you received: 12345
# Введите код из Telegram

# Сессия сохранится в файл aggregator_session.session
```

### Настройка VK API

#### Шаг 1: Создание приложения VK

1. Откройте <https://vk.com/apps?act=manage>
2. Нажмите "Создать приложение"
3. Выберите "Standalone приложение"
4. Заполните название

#### Шаг 2: Получение токена доступа

**Простой способ (для чтения публичных данных):**

1. Откройте <https://vkhost.github.io/>
2. Выберите нужные права (wall, groups, photos, video)
3. Авторизуйтесь
4. Скопируйте токен → `VK_ACCESS_TOKEN`

**Официальный способ:**

1. Перейдите в настройки вашего приложения
2. Скопируйте Service Token или получите User Token через OAuth

#### Шаг 3: Проверка доступа

```bash
# Замените <TOKEN> на ваш токен
curl "https://api.vk.com/method/wall.get?domain=apiclub&count=1&access_token=<TOKEN>&v=5.131"
```

### Настройка источников

Файл `config/sources.yaml` содержит список источников для мониторинга.

#### Формат Telegram URL

```yaml
- type: telegram
  url: https://t.me/channel_username # Без @ в URL!
  enabled: true
```

Примеры валидных URL:

- ✅ `https://t.me/durov`
- ✅ `https://t.me/telegram`
- ❌ `https://t.me/@durov` (уберите @)
- ❌ `t.me/durov` (добавьте https://)

#### Формат VK URL

```yaml
- type: vk
  url: https://vk.com/apiclub # Можно public123 или club123
  enabled: true
```

Примеры валидных URL:

- ✅ `https://vk.com/apiclub`
- ✅ `https://vk.com/public1`
- ✅ `https://vk.com/club12345`
- ❌ `vk.com/apiclub` (добавьте https://)

## 📁 Структура проекта

```
content-aggregator-bot/
├── app/                      # Основной код приложения
│   ├── __init__.py
│   ├── __main__.py          # Точка входа
│   ├── config.py            # Настройки из .env
│   ├── orchestrator.py      # Главный управляющий модуль
│   ├── database/            # Работа с БД
│   │   ├── models.py        # SQLAlchemy модели
│   │   └── repository.py    # Слой доступа к данным
│   ├── models/              # Модели данных
│   │   └── content.py       # Post, Media и др.
│   ├── scrapers/            # Скраперы для платформ
│   │   ├── base.py          # Базовый класс
│   │   ├── telegram.py      # Telegram скрапер
│   │   ├── vk.py           # VK скрапер
│   │   └── factory.py       # Фабрика скраперов
│   ├── publishers/          # Публикаторы
│   │   └── telegram.py      # Публикация в Telegram
│   └── utils/               # Утилиты
│       ├── logger.py        # Логирование
│       ├── rate_limiter.py  # Rate limiting
│       └── validators.py    # Валидация данных
├── config/
│   └── sources.yaml         # Список источников
├── data/                    # База данных (создается автоматически)
├── logs/                    # Логи (создается автоматически)
├── .env                     # Переменные окружения (создайте сами)
├── .env.example             # Пример переменных
├── pyproject.toml           # Конфигурация проекта
└── README.md                # Этот файл
```

## 🔧 Настройка параметров

### Переменные окружения

| Переменная                       | Описание                     | По умолчанию                               |
| -------------------------------- | ---------------------------- | ------------------------------------------ |
| `TELEGRAM_BOT_TOKEN`             | Токен Telegram бота          | -                                          |
| `TELEGRAM_TARGET_CHAT_ID`        | ID целевого чата             | -                                          |
| `TELEGRAM_API_ID`                | API ID из my.telegram.org    | -                                          |
| `TELEGRAM_API_HASH`              | API Hash из my.telegram.org  | -                                          |
| `TELEGRAM_SESSION_NAME`          | Имя файла сессии             | `aggregator_session`                       |
| `VK_ACCESS_TOKEN`                | Токен VK API                 | -                                          |
| `VK_API_VERSION`                 | Версия VK API                | `5.131`                                    |
| `DATABASE_URL`                   | URL базы данных              | `sqlite+aiosqlite:///./data/aggregator.db` |
| `LOG_LEVEL`                      | Уровень логирования          | `INFO`                                     |
| `LOG_FILE`                       | Путь к файлу логов           | `logs/bot.log`                             |
| `SCRAPE_INTERVAL_SECONDS`        | Интервал проверки источников | `60`                                       |
| `RATE_LIMIT_REQUESTS_PER_MINUTE` | Лимит запросов/мин           | `30`                                       |
| `POST_CHECK_DELAY_SECONDS`       | Задержка перед публикацией   | `600` (10 мин)                             |

## 📊 Логирование

Логи записываются:

- В консоль (цветной вывод)
- В файл `logs/bot.log` (с ротацией по 10 МБ)

Уровни логирования:

- `DEBUG` - детальная отладка
- `INFO` - основные события
- `WARNING` - предупреждения
- `ERROR` - ошибки

## 🐛 Решение проблем

### Бот не запускается

```bash
# Проверьте установку зависимостей
uv sync

# Проверьте .env файл
cat .env

# Запустите с DEBUG логами
LOG_LEVEL=DEBUG python -m app
```

### "Telegram client not initialized"

- Проверьте `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`
- Удалите `*.session` файлы и попробуйте снова

### "VK API error"

- Проверьте `VK_ACCESS_TOKEN`
- Убедитесь, что токен имеет права `wall` и `groups`
- Проверьте доступность группы (она должна быть публичной)

### Посты не публикуются

- Проверьте, что бот добавлен в группу
- Проверьте права бота (должен быть администратором)
- Проверьте `TELEGRAM_TARGET_CHAT_ID` (должен начинаться с `-`)

### "Rate limit exceeded"

- Увеличьте `SCRAPE_INTERVAL_SECONDS`
- Уменьшите количество источников
- Уменьшите `RATE_LIMIT_REQUESTS_PER_MINUTE`

## 🔒 Безопасность

⚠️ **Важно:**

- Никогда не коммитьте `.env` файл в git
- Храните токены в безопасности
- Регулярно обновляйте зависимости
- Используйте разные токены для dev/prod

## 📝 Добавление новых платформ

Для добавления новой платформы:

1. Создайте новый скрапер в `app/scrapers/your_platform.py`:

```python
from app.scrapers.base import BaseScraper

class YourPlatformScraper(BaseScraper):
    async def initialize(self):
        # Инициализация
        pass

    async def fetch_recent_posts(self, limit=10):
        # Получение постов
        pass

    async def close(self):
        # Закрытие
        pass
```

1. Добавьте новый тип в `app/models/content.py`:

```python
class PlatformType(str, Enum):
    TELEGRAM = "telegram"
    VK = "vk"
    YOUR_PLATFORM = "your_platform"  # Добавьте
```

1. Обновите фабрику в `app/scrapers/factory.py`

2. Добавьте валидацию в `app/utils/validators.py`

## 🤝 Поддержка

Если возникли вопросы или проблемы:

1. Проверьте логи в `logs/bot.log`
2. Включите `LOG_LEVEL=DEBUG` для детальной диагностики
3. Создайте Issue в репозитории
